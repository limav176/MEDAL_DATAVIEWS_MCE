{{ 
	standard_config(
		model_name='stg_sms_sfi',
		zone='bronze',
		materialized='ephemeral'
	)
}}

SELECT  
    subscriberid,
	subscriberkey,
	mobilemessagetrackingid,
	eid,
	mid,
	mobile,
	messageid,
	keywordid,
	codeid,
	conversationid,
	campaignid,
	"sent",
	delivered,
	undelivered,
	unsub,
	optin,
	optout,
	inbound,
	outbound,
	cast(cast(createdatetime as varchar) as timestamp) as createdatetime,
    cast(cast(modifieddatetime as varchar) as timestamp) as modifieddatetime,
	cast(cast(actiondatetime as varchar) as timestamp) as actiondatetime,
	istest,
	mobilemessagerecurrenceid,
	responsetomobilemessagetrackingid,
	isvalid,
	invalidationcode,
	sendid,
	sendsplitid,
	sendsegmentid,
	sendjobid,
	sendgroupid,
	sendpersonid,
	smsstandardstatuscodeid,
	"description",
	"name",
	shortcode,
	sharedkeyword,
	ordinal,
	fromname,
	jbdefinitionid,
	jbactivityid,
	smsjobid,
	ingestion_date,
	ingestion_year,
	ingestion_month,
	ingestion_day,
	execution_date,
	execution_year,
	execution_month,
	execution_day 
FROM  
	{{ source('comunicacoes_bronze', 'sms_sfi') }}